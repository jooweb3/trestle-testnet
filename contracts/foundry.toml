[profile.default]
src = "src"
out = "out"
libs = ["node_modules"]
solc_version = "0.8.36"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200
via_ir = true
test = "test"
cache_path = "forge-cache"

remappings = [
    "@openzeppelin/contracts/=node_modules/@openzeppelin/contracts/",
]
